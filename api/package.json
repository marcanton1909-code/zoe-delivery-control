{
  "name": "zoe-delivery-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply zoe-delivery-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply zoe-delivery-db --remote"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest",
    "@cloudflare/workers-types": "latest"
  }
}
